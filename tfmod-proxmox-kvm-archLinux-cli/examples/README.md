# Project Examples
### Project: tfmod-proxmox-kvm-archlinux-cli
There is 1 examples included in this folder for the project:
1. bash-ahc
    - Uses BASH scripts to install a simple Python CLI application.

